import { InstitucionFilterPipe } from './institucion-filter.pipe';

describe('InstitucionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InstitucionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
